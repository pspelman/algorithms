// 1. Sorts - State the time complexity for each of the following sorts

// Bubble sort
    // best omega Ω(n)
    // average theta Θ(n^2)
    // worst 0(n^2)

// Insertion sort
    // best omega Ω(n)
    // average theta Θ(n^2)
    // worst 0(n^2)

// Merge sort
    // best omega Ω(n log(n))
    // average theta Θ(n log(n))
    // worst O(n log(n))

// Quick sort
    // best omega Ω(n log(n))
    // average theta Θ(n log(n))
    // worst 0(n^2)

// Selection sort
    // best omega Ω(n^2)
    // average theta Θ(n^2)
    // worst 0(n^2)

// Heap sort (extra)
    // best omega Ω(n log(n))
    // average theta Θ(n log(n))
    // worst O(n log(n))



// 2. Write the class function for Heap()
        // include the method for inserting value into the heap


// 3. Hash Tables: Demonstrate an understanding of what hash tables are and how they work
    //a couple other names for hash tables are ____________________ or ________________________
    // this data structure is made up of ________________ pairs (Key - value)

    //pseudocode for adding a value to a hash table

// 4. Sorts: Choose a sort and implement it

function somethignSort(arr) {

}


// 5. Given a total amoutn of cents and a dictionary of that currency's coins and associated values, write a funciton that calculates the fewest number of coins and returns a dictionary of the calculated values

function generateChange(cents, changeDict) {

}